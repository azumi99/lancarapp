import { Text } from "@/components/ui/text";
import { SafeAreaView } from "react-native";

const ProfileScreen = () => {
    return (
        <SafeAreaView className="flex-1 items-center justify-center">
            <Text className="self-center ">haloo</Text>
        </SafeAreaView>
    )
}

export default ProfileScreen;